// Definindo a interface BalanceResult
export interface BalanceResult {
  // limite é um número que representa o limite de crédito de uma pessoa
  limite: number;
  // Saldo é um número que representa o saldo atual de uma pessoa
  Saldo: number;
}
